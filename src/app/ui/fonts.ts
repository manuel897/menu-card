import { Abril_Fatface, Alfa_Slab_One, Inter, Pridi } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const abrilFatface = Abril_Fatface({
  subsets: ['latin'],
  weight: '400',
});
export const pridi = Pridi({
  subsets: ['latin'],
  weight: '400',
});
export const alfaSlabOne = Alfa_Slab_One({
  subsets: ['latin'],
  weight: '400',
});